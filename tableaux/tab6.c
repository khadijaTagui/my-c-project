#include<stdio.h>
int main(){
    int n;
        printf("entrer le nombre d'elements:");
        scanf("%d",&n);
      int  tab[i];
    for(int i=0;i<n;i++){
        printf("entrer le nombre%d:",i+1);
        scanf("%d",&tab[i]);
    }
    mult = [0];
    for (int i=0;i)

return 0;
}